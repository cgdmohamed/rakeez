# Rakeez Platform Environment Variables Template
# Copy this to .env and fill in your values

# ============================================
# DATABASE (Required)
# ============================================
DATABASE_URL=postgresql://user:password@host:port/database

# ============================================
# FILE STORAGE
# ============================================

# Option 1: Google Cloud Storage (GCS) - Recommended
# These are auto-set in Replit development environment
PUBLIC_OBJECT_SEARCH_PATHS=/bucket-name/public
PRIVATE_OBJECT_DIR=/bucket-name/.private
DEFAULT_OBJECT_STORAGE_BUCKET_ID=bucket-name

# GCS Credentials (REQUIRED for PRODUCTION deployments only)
# Add these as deployment secrets, not workspace secrets
# GCS_PROJECT_ID=your-gcs-project-id
# GCS_CLIENT_EMAIL=service-account@project.iam.gserviceaccount.com
# GCS_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYour-Key-Here\n-----END PRIVATE KEY-----\n

# Option 2: Database Base64 Storage
# No additional environment variables needed
# Images stored directly in PostgreSQL as base64

# ============================================
# PAYMENT GATEWAYS
# ============================================

# Moyasar (Cards, mada, Apple Pay)
MOYASAR_PUBLIC_KEY=pk_test_your_public_key
MOYASAR_SECRET_KEY=sk_test_your_secret_key

# Tabby (Buy Now Pay Later)
# TABBY_PUBLIC_KEY=your_tabby_public_key
# TABBY_SECRET_KEY=your_tabby_secret_key

# ============================================
# COMMUNICATION SERVICES
# ============================================

# Twilio (SMS for OTP and notifications)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# SMTP Email (Optional - for receipts)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Expo Push Notifications (Optional - for mobile)
# EXPO_ACCESS_TOKEN=your_expo_access_token

# ============================================
# CACHING & SESSION (Optional)
# ============================================

# Redis (Optional - for sessions, rate limiting, OTP storage)
# REDIS_URL=redis://localhost:6379

# ============================================
# APPLICATION
# ============================================

# Environment
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Application URL
APP_URL=http://localhost:5000

# ============================================
# DEVELOPMENT
# ============================================

# Port (auto-set to 5000 in Replit)
PORT=5000

# Log Level
LOG_LEVEL=info

# ============================================
# NOTES
# ============================================
# 
# For Development:
# - Most object storage variables are auto-set by Replit
# - No GCS credentials needed (uses sidecar)
# 
# For Production (Published Domain):
# - Add GCS credentials as DEPLOYMENT SECRETS
# - See STORAGE_OPTIONS_GUIDE.md for detailed setup
# 
# Security:
# - Never commit .env to version control
# - Use strong, unique values for all secrets
# - Rotate credentials regularly
