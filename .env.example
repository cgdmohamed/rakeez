# App Configuration
NODE_ENV=development
PORT=5000
APP_URL=http://localhost:5000
APP_BASE_URL=http://localhost:5000
API_BASE_URL=http://localhost:5000

# CORS Configuration - REQUIRED for security
# Comma-separated list of allowed origins (no spaces)
# Development: Include localhost for testing
# Production: Only include your actual domains
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000,https://yourdomain.com,https://www.yourdomain.com,https://admin.yourdomain.com

# WebSocket Configuration
# For development: ws://localhost:5000/ws
# For production: wss://yourdomain.com/ws
VITE_WS_URL=ws://localhost:5000/ws
WS_URL=ws://localhost:5000/ws

# Database Configuration (Provided by Replit)
# DATABASE_URL=postgresql://user:password@host:port/database
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=password
# PGDATABASE=rakeez

# JWT Configuration - CRITICAL SECURITY
# IMPORTANT: Generate strong secrets using:
# node -e "console.log(require('crypto').randomBytes(96).toString('base64'))"
# Minimum length: 32 characters
# Recommended: 96-byte base64-encoded strings
JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-minimum-32-chars
SESSION_SECRET=your-super-secret-session-key-change-in-production-minimum-32-chars

# Redis Configuration (Optional in development, recommended for production)
REDIS_URL=redis://localhost:6379

# Email/SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Twilio SMS Configuration
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Moyasar Payment Gateway
MOYASAR_SECRET_KEY=sk_test_moyasar_secret_key
MOYASAR_PUBLISHABLE_KEY=pk_test_moyasar_public_key
MOYASAR_API_URL=https://api.moyasar.com
MOYASAR_WEBHOOK_SECRET=moyasar_webhook_secret

# Tabby BNPL Payment Gateway
TABBY_SECRET_KEY=sk_tabby_test_secret_key
TABBY_API_URL=https://api.tabby.ai
TABBY_MERCHANT_CODE=rakeez_sa
TABBY_WEBHOOK_SECRET=tabby_webhook_secret

# Push Notifications
EXPO_ACCESS_TOKEN=your-expo-access-token
FCM_SERVER_KEY=your-fcm-server-key
APNS_KEY=your-apns-key

# Object Storage (Provided by Replit)
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=bucket-id
# PUBLIC_OBJECT_SEARCH_PATHS=public
# PRIVATE_OBJECT_DIR=.private
